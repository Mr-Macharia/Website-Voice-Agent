{
  "name": "voice-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && uv run python server.py",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:worker": "cd backend && uv run python livekit_worker.py dev",
    "typecheck": "cd frontend && npm run typecheck",
    "test:voice": "cd backend && uv run python scripts/voice.py"
  }
}
